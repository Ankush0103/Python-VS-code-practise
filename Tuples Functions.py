# Creating a tuple using ()
t = (1, 2, 4, 5, 4, 1, 2, 1, 1)

print(t.count(1)) # returns the number of occurence of values.
print(t.index(5)) # returns the index of a given number. In caee of asimilar number the earlier index is returned same as lists.

