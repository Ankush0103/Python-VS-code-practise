
class Sample:
    # def __init__(self, name):
        # self.name = name
    def __init__(Ankur, name): # Hence self is only a parameter it can be changed to other name also. 
        Ankur.name = name

obj = Sample("Harry")
print(obj.name)
